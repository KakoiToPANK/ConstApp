<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Diplom"
        tools:targetApi="31">
        <activity
            android:name=".courseUseActivity"
            android:exported="false"
            android:label="courseUseActivity"
            android:theme="@style/Theme.Diplom" />
        <activity
            android:name=".PluggerActivity"
            android:exported="false"
            android:label="@string/title_activity_plugger"
            android:theme="@style/Theme.Diplom" />
        <activity
            android:name=".constructorActivity"
            android:exported="false"
            android:label="constructorActivity"
            android:theme="@style/Theme.Diplom" />
        <activity
            android:name=".mainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" /> <!-- Главная Activity -->
                <category android:name="android.intent.category.LAUNCHER" /> <!-- Иконка в лаунчере -->
            </intent-filter>
        </activity>
    </application>

</manifest>